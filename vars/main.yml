winrun_user: '{{ ansible_user }}'  # name of the Windows user
winrun_password: '{{ ansible_password }}'  # password of the Windows user
winrun_path: 'calc.exe'  # can also be: /cygdrive/c/WINDOWS/system32/calc.exe or C:/WINDOWS/system32/calc.exe
winrun_args: ''
winrun_force: false
